# State markers

Create construction, unverified, warning, and flag props as separate small silhouettes. Colour is
supporting evidence only; each state must remain distinguishable without relying on colour alone.
Do not bake labels into the image.
