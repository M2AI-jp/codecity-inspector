# Subscription provider handoff

This prompt may be used only through an explicitly configured local subscription command with
the required subscription confirmation guard. Do not request or use an API key. Do not fall back
to a paid API. Save every result to pending. If the command or billing boundary is unknown, create
a job pack instead.
