# Manual import handoff

Keep the source file unchanged. Import only after checking file existence and actual image format.
Record source, note, content hash, asset id, and unknown visual/provenance questions. Copy the
candidate to pending; never copy it to approved or export it automatically.
