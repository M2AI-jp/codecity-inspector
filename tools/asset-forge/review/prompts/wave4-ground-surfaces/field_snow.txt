Create a flat-color pixel-art production source for a seamless top-down snow ground texture in CodeCity Inspector. Match the supplied CodeCity winter palette and crisp pixel language, but use a deliberately simple tileable ground treatment. IMPORTANT: use one perfectly flat pale blue-grey background color with absolutely no lighting gradient, vignette, smooth shading, blur, or atmospheric haze. Cover about 20–35 percent of the surface with irregular, widely distributed blue-grey wind-drift and shallow ice-shadow forms. Build those forms only from hard-edged stepped pixel clusters, with each form broad and organic, and with visible source-scale blocks large enough to become 2–8 pixel clusters after reduction to a 64×64 tile. Use 8–12 flat colors total, all low-contrast and closely related. Distribute many differently shaped forms across the full image so no single form becomes a landmark. Let some forms cross each edge naturally; keep edge density balanced. No footprints, tracks, paths, dots, isolated marks, symbols, arrows, cracks, rocks, pebbles, stars, speckles, noise, stippling, diagonal bands, checkerboards, repeated stamps, regular spacing, corner motifs, or focal point. No buildings, trees, characters, objects, text, border, frame, perspective, horizon, or sprite-sheet layout. Opaque. The result must support this exact pipeline: crop an approximately 256×256 representative area, nearest-neighbor reduce to 32×32, nearest-neighbor enlarge to 64×64 so all final shapes use crisp 2-pixel blocks, then quantize to 8–12 colors. The final tile will repeat in a deterministic four-phase 12×8 field, so avoid any recurring landmark or directional stripe.
