# WorldPlan harness

Internal minimal frontend that consumes only serialized `WorldPlan` data and
uses geometric placeholders to verify generated terrain, routes, plots,
occupancy, interiors, NPCs, and evidence states. It is not the shipping game.

