# Browser acceptance

Real-browser evidence for the complete playable investigation loop and ordinary
RPG feel.

`serve-test-only.mjs` starts the real shipping composition and runtime against
`fixtures/diagnostic-art.fixture.mjs`. This is mechanical QA only: the sheet is not
product art, never enters `ship/50-art`, and cannot satisfy the product KGI or
human approval gate.
