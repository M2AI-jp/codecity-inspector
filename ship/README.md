# Shipping organization

Everything here is customer-deliverable. Runtime code must not import from
`studio/`. Modules communicate through versioned serializable contracts and
their public `index.mjs` only.

