# 40 World Generation

Deterministically turns a `TownModel` into a logical `WorldPlan`. Repository
identity fixes terrain, roads, topology, and all plots; current contents change
only occupancy and life.

