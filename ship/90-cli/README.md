# 90 CLI

The composition root for `npx codecity`. It orchestrates the public module
interfaces, builds an allowlisted immutable in-memory snapshot, and starts the
loopback server. The product path creates no distribution directory and writes
nothing into or beside the inspected repository.
